---
description: "Verification protocols: proportional checks, shell validation, browser verification, and blocker handling."
alwaysApply: false
---

# Verification Protocols

Code is not done until it has been verified at the right level for the task.

## Proportional Verification

| Effort level | Expected verification |
|--------------|-----------------------|
| **Instant** | Re-read or run one targeted check |
| **Light** | Targeted lint, typecheck, build, or focused test |
| **Deep** | Build plus relevant tests and behavior checks |
| **Exhaustive** | Full relevant validation, including UI/API/runtime checks |

Do not run a full suite for a typo fix. Do not skip behavior checks for user-facing changes.

## Typical Verification Order

```text
1. Fastest relevant static check
2. Focused behavior check
3. Broader validation only if the change warrants it
```

## Shell-Based Checks

Use the smallest useful command first:

- JavaScript/TypeScript: `lint`, `typecheck`, `build`, targeted tests
- Python: `ruff`, targeted `pytest`, import or compile checks
- Go: `go build`, `go test`, `go vet`
- Rust: `cargo check`, `cargo test`, `cargo clippy`
- Flutter: `flutter analyze`, `flutter test`
- Swift: `swift build`, `swift test`, `xcodebuild` when applicable

## Browser and Surface Checks

Use browser verification when the user would experience the change visually or interactively:
- layout-sensitive UI changes
- flow or navigation regressions
- console or network failures
- end-to-end behavior the shell cannot prove

## Self-Review

For non-trivial work, ask:

```text
What would break this?
What assumptions might be wrong?
Is there a simpler way?
Did I solve the actual request?
```

## When Verification Fails

Use an evidence-first loop:

```text
What failed?
What is the best current hypothesis?
What is the smallest corrective change?
What check will prove the fix?
```

After repeated failed attempts, stop guessing and use current docs or web search.
